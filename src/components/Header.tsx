import { Typography } from '@mui/material';

const Header = () => {
  return (
    <Typography variant="h4" color="primary" noWrap sx={{ padding: 2 }}>
      Data Visualization Demo
    </Typography>
  );
};

export default Header;
